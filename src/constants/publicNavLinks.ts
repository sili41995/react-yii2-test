import { NavLinks } from '@/types/types';
import PagePaths from './pagePaths';

const publicNavLinks: NavLinks = [
  {
    href: `/${PagePaths.about}`,
    title: 'About',
  },
];

export default publicNavLinks;
